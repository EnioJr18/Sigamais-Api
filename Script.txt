SET TIME ZONE '-03:00';


-- --------------------------------------------------------
-- Banco de dados: sigamais (Estrutura PostgreSQL)
-- --------------------------------------------------------


--
-- Estrutura para tabela usuario
--
CREATE TABLE usuario (
  id SERIAL NOT NULL,
  nome VARCHAR(255) NOT NULL,
  cpf CHAR(11) NOT NULL,
  email VARCHAR(255) NOT NULL,
  senha VARCHAR(255) NOT NULL,
  perfil VARCHAR(50) NOT NULL,
  criado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  atualizado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);


--
-- Estrutura para tabela aluno
--
CREATE TABLE aluno (
  id SERIAL NOT NULL,
  usuario_id INTEGER NOT NULL,
  matricula VARCHAR(50) NOT NULL,
  curso VARCHAR(150) NOT NULL,
  renda_familiar DECIMAL(10,2) DEFAULT NULL,
  ano_ingresso INTEGER NOT NULL,
  status VARCHAR(20) NOT NULL,
  criado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  atualizado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);


--
-- Estrutura para tabela professor
--
CREATE TABLE professor (
  id SERIAL NOT NULL,
  usuario_id INTEGER NOT NULL,
  titulacao VARCHAR(100) NOT NULL,
  criado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  atualizado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);


--
-- Estrutura para tabela disciplina
--
CREATE TABLE disciplina (
  id SERIAL NOT NULL,
  nome VARCHAR(150) NOT NULL,
  carga_horaria INTEGER NOT NULL,
  pre_requisito_id INTEGER DEFAULT NULL,
  criado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  atualizado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);


--
-- Estrutura para tabela turma
--
CREATE TABLE turma (
  id SERIAL NOT NULL,
  disciplina_id INTEGER NOT NULL,
  professor_id INTEGER NOT NULL,
  semestre VARCHAR(20) NOT NULL,
  vagas INTEGER NOT NULL,
  criado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  atualizado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);


--
-- Estrutura para tabela matricula
--
CREATE TABLE matricula (
  id SERIAL NOT NULL,
  aluno_id INTEGER NOT NULL,
  turma_id INTEGER NOT NULL,
  status VARCHAR(20) NOT NULL,
  criado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  atualizado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);


--
-- Estrutura para tabela nota
--
CREATE TABLE nota (
  id SERIAL NOT NULL,
  matricula_id INTEGER NOT NULL,
  valor DECIMAL(5,2) NOT NULL,
  tipo VARCHAR(50) NOT NULL,
  criado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  atualizado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);


--
-- Estrutura para tabela frequencia
--
CREATE TABLE frequencia (
  id SERIAL NOT NULL,
  matricula_id INTEGER NOT NULL,
  faltas INTEGER NOT NULL,
  criado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  atualizado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);


--
-- Estrutura para tabela alerta
--
CREATE TABLE alerta (
  id SERIAL NOT NULL,
  aluno_id INTEGER NOT NULL,
  nivel_risco VARCHAR(50) NOT NULL,
  motivo VARCHAR(255) NOT NULL,
  data_alerta DATE NOT NULL,
  criado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  atualizado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);




-- --------------------------------------------------------
-- ÍNDICES (Chaves Primárias, Únicas e Índices de Performance)
-- --------------------------------------------------------


ALTER TABLE usuario
  ADD PRIMARY KEY (id),
  ADD CONSTRAINT uk_usuario_cpf UNIQUE (cpf),
  ADD CONSTRAINT uk_usuario_email UNIQUE (email);


ALTER TABLE aluno
  ADD PRIMARY KEY (id),
  ADD CONSTRAINT uk_aluno_usuario UNIQUE (usuario_id),
  ADD CONSTRAINT uk_aluno_matricula UNIQUE (matricula);


ALTER TABLE professor
  ADD PRIMARY KEY (id),
  ADD CONSTRAINT uk_professor_usuario UNIQUE (usuario_id);


ALTER TABLE disciplina
  ADD PRIMARY KEY (id);
CREATE INDEX idx_disciplina_prereq ON disciplina (pre_requisito_id);


ALTER TABLE turma
  ADD PRIMARY KEY (id);
CREATE INDEX idx_turma_disciplina ON turma (disciplina_id);
CREATE INDEX idx_turma_professor ON turma (professor_id);


ALTER TABLE matricula
  ADD PRIMARY KEY (id),
  ADD CONSTRAINT uk_matricula_aluno_turma UNIQUE (aluno_id, turma_id);
CREATE INDEX idx_matricula_aluno ON matricula (aluno_id);
CREATE INDEX idx_matricula_turma ON matricula (turma_id);


ALTER TABLE nota
  ADD PRIMARY KEY (id);
CREATE INDEX idx_nota_matricula ON nota (matricula_id);


ALTER TABLE frequencia
  ADD PRIMARY KEY (id);
CREATE INDEX idx_frequencia_matricula ON frequencia (matricula_id);


ALTER TABLE alerta
  ADD PRIMARY KEY (id);
CREATE INDEX idx_alerta_aluno ON alerta (aluno_id);




-- --------------------------------------------------------
-- RESTRIÇÕES (CHAVES ESTRANGEIRAS)
-- --------------------------------------------------------


ALTER TABLE aluno
  ADD CONSTRAINT fk_aluno_usuario FOREIGN KEY (usuario_id) REFERENCES usuario (id) ON DELETE CASCADE;


ALTER TABLE professor
  ADD CONSTRAINT fk_professor_usuario FOREIGN KEY (usuario_id) REFERENCES usuario (id) ON DELETE CASCADE;


ALTER TABLE disciplina
  ADD CONSTRAINT fk_disciplina_prereq FOREIGN KEY (pre_requisito_id) REFERENCES disciplina (id) ON DELETE SET NULL;


ALTER TABLE turma
  ADD CONSTRAINT fk_turma_disciplina FOREIGN KEY (disciplina_id) REFERENCES disciplina (id),
  ADD CONSTRAINT fk_turma_professor FOREIGN KEY (professor_id) REFERENCES professor (id);


ALTER TABLE matricula
  ADD CONSTRAINT fk_matricula_aluno FOREIGN KEY (aluno_id) REFERENCES aluno (id) ON DELETE CASCADE,
  ADD CONSTRAINT fk_matricula_turma FOREIGN KEY (turma_id) REFERENCES turma (id) ON DELETE CASCADE;


ALTER TABLE nota
  ADD CONSTRAINT fk_nota_matricula FOREIGN KEY (matricula_id) REFERENCES matricula (id) ON DELETE CASCADE;


ALTER TABLE frequencia
  ADD CONSTRAINT fk_frequencia_matricula FOREIGN KEY (matricula_id) REFERENCES matricula (id) ON DELETE CASCADE;


ALTER TABLE alerta
  ADD CONSTRAINT fk_alerta_aluno FOREIGN KEY (aluno_id) REFERENCES aluno (id) ON DELETE CASCADE;